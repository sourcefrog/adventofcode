enum Dir {
    Left, 
    Right,
}

struct Step {
    dir: Dir,
    len: usize,
}

fn load() -> Vec<Step> {
    todo!()
}

fn solve_a() -> usize {
    0
}

pub fn main() {
    println!("{}", solve_a());
}
